{
  "private": "true",
  "name": "drinkit",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n \"Server,Client\" -c \"bgCyan,bgMagenta\" \"yarn workspace client dev\" \"yarn workspace server dev\"",
    "start": "concurrently --kill-others-on-fail \"yarn workspace client start\" \"yarn workspace server start\""
  },
  "dependencies": {
    "concurrently": "^6.0.1"
  },
  "devDependencies": {
    "eslint": "^7.23.0"
  }
}
